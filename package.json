{
  "name": "pixora",
  "version": "0.1.0",
  "private": true,
  "description": "Automated AI-assisted website generation and deployment pipeline for small businesses.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run start --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
